Compilation notes for arm devices
=================================

- Compiling some versions of the image on raspberry pi 4 in tests takes about __25+ minutes__.
- If you run into compiler issues on arm devices like the raspberry pi, you should be able to checkout a different commit for the repo, the trick is to find one that _will_ compile.
